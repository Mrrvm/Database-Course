1) 
	SELECT branch_name
	FROM branch
	WHERE branch_city LIKE "Brooklyn";

2)
	SELECT account_number, branch.branch_name
	FROM branch, account
	WHERE branch_city LIKE "Brooklyn"
	AND account.branch_name = branch.branch_name;

3)
	SELECT customer_name, account.account_number, branch.branch_name
	FROM branch, account, depositor
	WHERE branch_city LIKE "Brooklyn"
	AND account.branch_name = branch.branch_name
	AND account.account_number = depositor.account_number;

4)
	
5)
